<!DOCTYPE html>
<html>
  <head>
    <title>ProjetRuby</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>

    <header>
      <div>
          <h1><%= link_to "ReservePlane", "/", class: "title" %></h1>
        </div>

      <div class="link-menu">
        <ul>
          <li><%= link_to "Mes réservations", "/reservations" %></li> |
          <li><%= link_to "Vols", "/vols" %></li>
          <li><a href=""></a></li>
        </ul>
      </div>

       <div>
         <% if user_signed_in? %>
           Logged in as <strong><%= current_user.email %></strong>.
           <%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link' %> |
           <%= button_to "Deconnexion", destroy_user_session_path, :method => :delete, :data => {:no_turbolink => true} %>

         <% else %>
           <%= link_to "Sign up", new_user_registration_path, :class => 'navbar-link'  %> |
           <%= link_to "Login", new_user_session_path, :class => 'navbar-link'  %>
         <% end %>
       </div>
    </header>
    <div class="main">
      <%= yield %>
    </div>






    <% if notice %>
    <p class="alert alert-success"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert alert-danger"><%= alert %></p>
    <% end %>
  </body>
</html>
